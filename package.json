{
    "name": "myswisstoolbox-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd frontend && npm install && npm run build; cd ..; rm -rf public; mv frontend/dist public",
        "install": "cd frontend && npm install"
    }
}